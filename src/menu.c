
void date_input(int *year, int *month, int *day, int *hour, int *minute, int *second)
{


	bool first_input = true;

	const char date_template = "%d-%d-%d %d:%d:%d";

	if (first_input)
	{

		size_t default_time_size = 1 + snprintf(NULL, 0, date_template, );
		date_template


		printf("Default date is ");
	}
	else
	{
		printf("Current date is %d-%d-%d %d:%d:%d");
	}
}
